node_modules
.next
out
.env
.env.local
*.log
.DS_Store
.vercel
next-env.d.ts

# TypeScript incremental build cache — machine-specific, not source.
tsconfig.tsbuildinfo

# Hardcoded local Shopify credentials — never commit
